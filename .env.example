# Entorno de backend contra el que se comunica la app (Context.md 7.2).
# Copia este archivo como `.env` y ajusta el valor si tu backend no corre en el puerto por defecto.
VITE_API_BASE_URL=http://localhost:3000
