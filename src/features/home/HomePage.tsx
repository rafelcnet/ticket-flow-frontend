/**
 * Pantalla 2 — Home / Menú (`/home`, Context.md 5.3).
 * Versión inicial vacía: el perfil del usuario y el menú de acciones
 * se implementan en su ticket correspondiente.
 */
export const HomePage = () => (
  <section>
    <h1>Home</h1>
  </section>
)
