/**
 * Pantalla 1 — Login (`/login`, Context.md 5.2).
 * Versión inicial vacía: el formulario, la validación y la autenticación
 * se implementan en su ticket correspondiente.
 */
export const LoginPage = () => (
  <section>
    <h1>Login</h1>
  </section>
)
