/**
 * Constantes de configuración de la aplicación (SpecProject 2 · carpeta config/).
 * Sin lógica: sólo valores fijados en Context.md.
 */
export const APP_CONFIG = {
  /** Cargo por servicio del resumen de compra — Context.md 5.4 (Paso 4). */
  serviceFee: 8.0,
  /** Tamaño de página por defecto de GET /bookings — Context.md 5.5. */
  defaultPageSize: 10,
} as const
